{
  viewer {
    gists(first: 100, orderBy: { field: UPDATED_AT, direction: DESC }) {
      results: nodes {
        ... on Gist {
          name
          url
          description
        }
      }
    }
  }
}
